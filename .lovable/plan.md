

## План: Виправлення відображення авторів постів та видалення дублікату username

---

### Виявлені проблеми

#### 1. Деякі пости показують "Користувач" замість автора

**Причина знайдена в базі даних:**

| Post ID | user_id | author_name |
|---------|---------|-------------|
| 59990201... | c836df06... | Олександр Боднарюк ✅ |
| 240abe19... | **null** | null ❌ |
| 0154545f... | **null** | null ❌ |
| 97cdb112... | c836df06... | Олександр Боднарюк ✅ |

Пости з `user_id = null` не мають автора, тому показують fallback "Користувач".

**Рішення**: При створенні нового поста переконатися, що `user_id` завжди встановлюється. Для існуючих постів без автора - присвоїти їм поточного користувача якщо він їх створив.

#### 2. Username (@galyna) дублює ім'я

**Проблема**: Під ім'ям "Galyna Bodnariuk" показується "@galyna", що є зайвим повторенням.

**Рішення**: Прибрати рядок з `@username` з UI - залишити тільки `full_name` як у Facebook/Instagram.

---

### Технічні зміни

---

### Файл 1: `src/components/feed/NewsFeed.tsx`

#### 1.1 Виправити збереження user_id при створенні поста

**Рядок ~158-163**: Переконатися що `user_id` береться з Supabase Auth:

```tsx
const handleCreatePost = async () => {
  if (!newPostContent.trim() && !selectedFile) return;
  
  // Перевірка авторизації
  if (!currentUser?.id) {
    toast({ title: "Будь ласка, увійдіть в систему", variant: "destructive" });
    return;
  }

  setIsUploading(true);
  // ...
  const newPost = {
    content: newPostContent,
    user_id: currentUser.id,  // Завжди використовувати currentUser з Supabase
    // ...
  };
```

#### 1.2 Прибрати `username` з props - не потрібен

**Рядок 409**: Прибрати передачу `username`:

```tsx
author={{
  id: post.user_id,
  name: authorName,
  // username: getUsername(postAuthor),  ← ПРИБРАТИ
  avatarUrl: postAuthor?.avatar_url || postAuthor?.avatarUrl || '',
}}
```

---

### Файл 2: `src/components/feed/PostCard.tsx`

#### 2.1 Прибрати відображення @username

**Рядки 163-171**: Прибрати рядок з username:

**БУЛО:**
```tsx
<div className="flex flex-col">
  <span className="text-sm font-semibold">{author.name}</span>
  <div className="flex items-center gap-1">
    <span className="text-xs text-muted-foreground">@{author.username}</span>
    {author.profession && isCurrentUserInvestor && (
      <span className="profession-badge...">
        {author.profession}
      </span>
    )}
  </div>
</div>
```

**СТАНЕ:**
```tsx
<div className="flex flex-col">
  <span className="text-sm font-semibold">{author.name}</span>
  {/* Титули показуються тільки інвесторам, без @username */}
  {author.profession && isCurrentUserInvestor && (
    <span className="profession-badge...">
      {author.profession}
    </span>
  )}
</div>
```

#### 2.2 Оновити PostCardProps - зробити username опціональним

**Рядки 29-38**:

```tsx
export interface PostCardProps {
  id: string;
  author: {
    id: string;
    name: string;
    username?: string;  // Зробити опціональним (не використовується)
    avatarUrl?: string;
    profession?: string;
    isShareHolder?: boolean;
  };
  // ...
}
```

---

### Файл 3: `src/components/feed/NewsFeed.tsx` - Прибрати функцію getUsername

**Рядки 55-67**: Можна видалити функцію `getUsername()` оскільки вона більше не потрібна:

```tsx
// ВИДАЛИТИ:
// const getUsername = (user: any) => {
//   if (!user) return 'user';
//   const firstName = user.full_name?.split(' ')[0];
//   if (firstName) {
//     return firstName.toLowerCase();
//   }
//   return 'user';
// };
```

---

### Візуальна схема результату

```text
БУЛО:
┌─────────────────────────────────────┐
│ 🖼️ Galyna Bodnariuk                 │
│    @galyna  ← ЗАЙВЕ!                │
│ ─────────────────────────────────── │
│ [Контент поста]                     │
└─────────────────────────────────────┘

СТАНЕ (як у Facebook/Instagram):
┌─────────────────────────────────────┐
│ 🖼️ Galyna Bodnariuk                 │
│ ─────────────────────────────────── │
│ [Контент поста]                     │
└─────────────────────────────────────┘
```

---

### Підсумок змін

| Файл | Зміни |
|------|-------|
| `src/components/feed/NewsFeed.tsx` | Видалити функцію `getUsername()`, прибрати передачу `username` в PostCard, додати перевірку `currentUser?.id` при створенні поста |
| `src/components/feed/PostCard.tsx` | Прибрати відображення `@{author.username}`, зробити `username` опціональним в props |

---

### Додатково: Виправлення постів з null user_id

Для виправлення існуючих постів без автора, можна виконати SQL запит в адмін-панелі (Run SQL в Supabase):

```sql
-- Оновити пости без user_id - присвоїти конкретному користувачу
UPDATE posts 
SET user_id = 'c836df06-8923-468e-b4c1-9331cadbe183'  -- ID адміністратора
WHERE user_id IS NULL;
```

---

### Очікуваний результат

1. **Усі пости показують ім'я автора** - якщо `user_id` не null
2. **Без дублювання @username** - тільки повне ім'я як у соцмережах
3. **Нові пости завжди мають автора** - перевірка авторизації перед створенням

