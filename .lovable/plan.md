

## План: Покращення форми реєстрації з вимогами до паролю

---

### Проблема

1. Користувач бачить помилки паролю **англійською мовою** (від Supabase)
2. Немає **перемикача мов** на сторінці авторизації
3. Користувач не бачить **вимог до паролю** при введенні
4. Поточна валідація перевіряє тільки 6 символів, а Supabase вимагає 10+ з великими/малими літерами, цифрами і символами

---

### Рішення

#### Частина 1: Додати перемикач мов на сторінку авторизації

**Файл: `src/components/auth/AuthStepManager.tsx`**

Додати `LanguageSelector` у верхній правий кут форми авторизації:

```tsx
import { LanguageSelector } from '@/components/layout/LanguageSelector';

// У компоненті додати:
<div className="absolute top-4 right-4">
  <LanguageSelector />
</div>
```

---

#### Частина 2: Створити компонент візуалізації вимог до паролю

**Новий файл: `src/components/auth/PasswordRequirements.tsx`**

Компонент буде показувати список вимог з кольоровими індикаторами:

```tsx
interface PasswordRequirementsProps {
  password: string;
}

// Вимоги:
// ✓/✗ Мінімум 10 символів
// ✓/✗ Велика літера (A-Z)
// ✓/✗ Мала літера (a-z)
// ✓/✗ Цифра (0-9)
// ✓/✗ Спеціальний символ (!@#$%^&* тощо)
```

Кожна вимога буде:
- **Зелена галочка** ✓ якщо виконана
- **Червоний хрестик** ✗ якщо не виконана

---

#### Частина 3: Оновити переклади

**Файл: `src/lib/translations.ts`**

Додати нові ключі для всіх 5 мов:

```typescript
// Українська (uk)
passwordRequirements: "Вимоги до паролю:",
passwordMinChars: "Мінімум 10 символів",
passwordUppercase: "Велика літера (A-Z)",
passwordLowercase: "Мала літера (a-z)",
passwordNumber: "Цифра (0-9)",
passwordSpecial: "Спеціальний символ (!@#$%^&*)",
passwordTooWeak: "Пароль не відповідає вимогам безпеки",

// English (en)
passwordRequirements: "Password requirements:",
passwordMinChars: "Minimum 10 characters",
passwordUppercase: "Uppercase letter (A-Z)",
passwordLowercase: "Lowercase letter (a-z)",
passwordNumber: "Number (0-9)",
passwordSpecial: "Special character (!@#$%^&*)",
passwordTooWeak: "Password does not meet security requirements",

// ... аналогічно для pl, de, ro
```

---

#### Частина 4: Інтегрувати у форму реєстрації

**Файл: `src/components/auth/SupabaseRegisterForm.tsx`**

1. Імпортувати `PasswordRequirements`
2. Додати компонент після поля пароля
3. Оновити валідацію з 6 на 10 символів + інші вимоги
4. Перехоплювати англійські помилки від Supabase і показувати локалізовані

```tsx
// Додати під полем пароля:
<PasswordRequirements password={password} />

// Оновити валідацію:
const validatePassword = (pwd: string) => {
  return {
    minLength: pwd.length >= 10,
    hasUppercase: /[A-Z]/.test(pwd),
    hasLowercase: /[a-z]/.test(pwd),
    hasNumber: /[0-9]/.test(pwd),
    hasSpecial: /[!@#$%^&*(),.?":{}|<>]/.test(pwd),
  };
};

const isPasswordValid = Object.values(validatePassword(password)).every(Boolean);
```

---

### Візуалізація

```text
┌─────────────────────────────────────────┐
│                            🌐 [UK ▼]    │  ← Перемикач мов
├─────────────────────────────────────────┤
│         Зареєструватися                 │
│                                         │
│  [Ім'я          ] [Прізвище         ]   │
│  [Email                             ]   │
│  [Пароль                         👁 ]   │
│                                         │
│  Вимоги до паролю:                      │
│  ✓ Мінімум 10 символів                  │  ← Зелена (виконано)
│  ✓ Велика літера (A-Z)                  │  ← Зелена
│  ✗ Мала літера (a-z)                    │  ← Червона (не виконано)
│  ✓ Цифра (0-9)                          │  ← Зелена
│  ✗ Спеціальний символ (!@#$%^&*)        │  ← Червона
│                                         │
│  [Підтвердження паролю            👁 ]   │
│                                         │
│  [      Зареєструватися      ]          │
└─────────────────────────────────────────┘
```

---

### Файли для зміни

| Файл | Зміни |
|------|-------|
| `src/lib/translations.ts` | Додати переклади для вимог паролю |
| `src/components/auth/PasswordRequirements.tsx` | **НОВИЙ** - компонент візуалізації вимог |
| `src/components/auth/SupabaseRegisterForm.tsx` | Інтегрувати компонент, оновити валідацію |
| `src/components/auth/AuthStepManager.tsx` | Додати `LanguageSelector` |

---

### Очікуваний результат

1. Перемикач мов **доступний** на сторінці авторизації
2. Користувач бачить **вимоги до паролю** в реальному часі
3. **Кольорова індикація** - зелені галочки для виконаних вимог, червоні для невиконаних
4. Всі **повідомлення локалізовані** відповідно до обраної мови
5. Користувач **розуміє** що саме потрібно виправити в паролі

