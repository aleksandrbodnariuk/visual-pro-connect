

## План: Виправлення структури коментаря як у Facebook

---

### Аналіз скриншоту Facebook

```text
┌────────────────────────────────────┐
│  👤  Antonina Shyshkovska          │  ← Ім'я (жирний)
│      Супер                         │  ← Коментар (відразу під ім'ям)
│                                    │
│  3 дн.  Подобається  Відповісти    │  ← Дії (поза бульбашкою)
└────────────────────────────────────┘
```

---

### Поточна проблема

Після останньої зміни ім'я і коментар стали в одному рядку inline:
```
Antonina Shyshkovska Супер
```

А треба вертикально, але компактно:
```
Antonina Shyshkovska
Супер
```

---

### Рішення

**Файл:** `src/components/feed/CommentItem.tsx`

Повернути вертикальну структуру, але з нульовими відступами між рядками:

```tsx
<div className="bg-muted/50 rounded-2xl px-3 py-1.5 inline-block max-w-full">
  {/* Ім'я + бейдж на першому рядку */}
  <div className="flex items-center gap-1">
    <Link 
      to={`/profile/${comment.user_id}`} 
      className="font-semibold text-xs hover:underline"
    >
      {comment.user?.full_name || 'Користувач'}
    </Link>
    {isPostAuthor && (
      <Badge variant="secondary" className="text-[10px] py-0 px-1.5 h-4">
        Автор
      </Badge>
    )}
  </div>
  {/* Текст коментаря відразу під ім'ям (без margin-top) */}
  <p className="text-sm break-words">{comment.content}</p>
</div>
```

---

### Ключові зміни

| Елемент | Було | Стане |
|---------|------|-------|
| Структура | Все inline в одному `<p>` | Ім'я в `<div>`, текст в `<p>` (вертикально) |
| Відступ між ім'ям і текстом | - | **Без відступу** (0px) |
| Gap в div з ім'ям | `gap-2` (8px) | `gap-1` (4px) |

---

### Візуальне порівняння

```text
БУЛО (inline):                СТАНЕ (як у Facebook):
┌─────────────────────┐       ┌─────────────────────┐
│ Ім'я Супер          │       │ Ім'я                │
└─────────────────────┘       │ Супер               │
                              └─────────────────────┘
```

---

### Файл для редагування

| Файл | Зміни |
|------|-------|
| `src/components/feed/CommentItem.tsx` | Рядки 59-75: розділити ім'я і текст на окремі рядки без відступів |

---

### Очікуваний результат

1. Ім'я користувача на першому рядку
2. Текст коментаря відразу під ім'ям (без проміжку)
3. "Подобається" та "Відповісти" відразу під бульбашкою
4. Компактний вигляд як у Facebook

